export interface IUser {
    id?: number,
    username: string,
    password: string,
    email?: string,
    firstName?: string,
    lastName?: string,
    phone?: string,
    acceptterms?: boolean,
}